# PII: real driver-card data and parsed dumps
*.ddd
output.json
output.err

# macOS junk
.DS_Store

# Local reference clone of tachoparser (pulled as a Go module via go get,
# kept here for source inspection only). Not part of this repo.
/tachoparser/

# tacho-ui build artefacts
/tacho-ui/build/bin/
/tacho-ui/tacho-ui
/tacho-ui/frontend/dist/
/tacho-ui/frontend/node_modules/
/tacho-ui/frontend/package.json.md5

# Local-only env (Google Maps API key, etc.)
/tacho-ui/frontend/.env
/tacho-ui/frontend/.env.local

# Editor / OS noise
*.swp
*.swo
Thumbs.db
